# Hotspot consent screenshots

Device evidence for PR #828 (Pixel 9a, Android 16 / API 37). Kept on this
standalone branch so the screenshots are viewable from the pull request
without adding binaries to the project history.
